This project simulates a single server queue.

Build the Makefile using:
    ```opp_makemake```

Then run make:
    ```make```

Then run by right clicking the Eclipse project -> Run As -> OMNeT++ Simulation
